{
  "private": true,
  "scripts": {
    "format": "prettier --write ./src/*.ts",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "@sendgrid/mail": "^6.1.4",
    "graphcool-lib": "^0.0.3",
    "stripe": "^5.1.1",
    "validator": "^9.1.2"
  },
  "devDependencies": {
    "lint-staged": "^5.0.0",
    "prettier": "^1.8.2",
    "tslint": "^5.8.0",
    "typescript": "^2.6.1"
  }
}
